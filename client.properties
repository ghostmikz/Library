# NMIT Library Client Configuration
# Set server.host to the LAN IP of the machine running the server.
# User-saved changes are stored in settings.properties (not committed).

server.host=localhost
server.port=9091
language=mn
